# ── PAAI Requirements ── Python 3.13 + Windows compatible (no compilation) ──

# Core Flask
flask==3.1.0
flask-sqlalchemy==3.1.1
flask-wtf==1.2.2
flask-login==0.6.3
flask-migrate==4.1.0
flask-mail==0.10.0
flask-bcrypt==1.0.1
flask-cors==5.0.0

# Database — pg8000 is pure Python, zero compilation needed
pg8000==1.31.2
sqlalchemy==2.0.40
alembic==1.15.2

# Forms & validation
wtforms==3.2.1
email-validator==2.2.0

# Environment & security
python-dotenv==1.1.0
markupsafe==3.0.2

# Image handling
pillow==11.2.1

# AI Chatbot (optional — comment out if not using)
openai==1.77.0

# Production server (Linux/Mac only — skip on Windows dev)
gunicorn==23.0.0
